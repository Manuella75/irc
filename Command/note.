> PART canal plus join avec le bon msg
> Join + topic en 2 eme arg 
> cmd topic 
> test arg User  + fix (mode , channel) user ne peux pas etre dans plusieurs chanel
> cmd User
> mode avec juste super user et user classic
  > kick
> changer les msg pour que ce soit clean
> list pour voir les chann dispo 